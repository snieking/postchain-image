version: '3.9'

services:
  example:
    container_name: example
    image: snieking/postchain:latest
    volumes:
      - ./rell:/usr/src/rell
      - ./data:/var/lib/postgresql/data
    ports:
      - 7740:7740
